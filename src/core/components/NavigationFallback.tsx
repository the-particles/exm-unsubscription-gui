import { Spinner } from '@pars/shared/components/ui/spinner'

const NavigationFallback = () => (
  <div className="w-full h-full grow flex justify-center items-center">
    <Spinner />
  </div>
)

export default NavigationFallback
